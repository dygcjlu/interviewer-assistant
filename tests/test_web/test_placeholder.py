"""
完成标准（待 web-layer Teammate 完成后补充）：
- POST /api/resume/upload 返回 candidate_id + questions
- POST /api/interview/start 返回 200 且 session stage=interviewing
- WebSocket /ws/interview 连接后收到 status 消息
- 错误响应格式为 {"error": {"code": ..., "message": ..., "detail": ...}}
"""
import pytest


def test_placeholder():
    """占位：web-layer 模块实现后替换为真实测试。"""
    pass
