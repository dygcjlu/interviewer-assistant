"""
完成标准（待 infra-storage Teammate 完成后补充）：
- CandidateRepository.create/get 返回 CandidateProfile dataclass（非 dict）
- InterviewRepository.save 在事务中执行
- EvalReport 可写入并按 interview_id 查询
- TokenUsage 可追加写入，不覆盖历史记录
"""
import pytest


def test_placeholder():
    """占位：infra-storage 模块实现后替换为真实测试。"""
    pass
