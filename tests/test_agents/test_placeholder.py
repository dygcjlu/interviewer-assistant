"""
完成标准（待 agent-framework Teammate 完成后补充）：
- Orchestrator 可创建新会话
- switch_agent() 满足前置条件时切换成功，不满足时抛出 SessionError
- InterviewAgent 的 SuggestionTrigger 在候选人沉默 2s 后自动触发
- 三个 Agent 的 run()/stop() 不阻塞事件循环
"""
import pytest


def test_placeholder():
    """占位：agent-framework 模块实现后替换为真实测试。"""
    pass
