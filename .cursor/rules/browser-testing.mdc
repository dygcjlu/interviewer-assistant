---
description: 使用 cursor-ide-browser MCP 对本项目进行端到端浏览器测试的方法与注意事项。
alwaysApply: false
---

# 浏览器测试方法（cursor-ide-browser）

## 使用前提

1. 后端服务已启动，默认监听 `http://127.0.0.1:8001`。
2. Cursor IDE 已启用 `cursor-ide-browser` MCP Server。

## 基本工作流

```
browser_navigate(url)                           # 打开页面
browser_lock()                                  # 锁定浏览器（阻止用户干扰）
browser_snapshot()                              # 获取可交互元素树（取 ref）
browser_click(ref) / browser_fill(ref, value)   # 交互
browser_take_screenshot()                       # 截图确认视觉效果
browser_unlock()                                # 释放浏览器
```

## 注意事项

- **Quasar select 组件**（`ui.select`）无法通过坐标点击打开下拉列表，需通过检查服务器日志（`logs/backend.out.log`）确认 API 调用是否正确。
- **`on("update:model-value", ...)` vs `on_value_change`**：NiceGUI select 的 `e.args` 返回 Quasar 原始事件对象（含整数索引），应使用 `on_value_change` 并读取 `e.value`。
- 后端日志是验证修复的可靠手段，可代替浏览器自动化点击验证。
- 每次交互前先调用 `browser_snapshot()` 取最新 `ref`，不要复用旧 ref。
- `_load_candidates()` 等 httpx 调用是**服务端**发起的，不会出现在浏览器 Network 面板，需在服务器日志中确认。

## 典型测试场景

| 场景 | 验证方式 |
|---|---|
| 选择历史候选人 | `logs/backend.out.log` 中出现 `GET /api/resume/profile?candidate_id=<uuid>`（而非 `{'value':...}`） |
| 上传简历后下拉刷新 | 上传完成后再次调用 `GET /api/candidates?limit=50`，且 snapshot 中 select 出现新候选人 |
| API 返回错误 | `browser_snapshot()` 中 chat 区域出现红色错误卡片 |
